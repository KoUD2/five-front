import { FC } from 'react'

import styles from './Progress_cards.module.scss'

const Finish_card: FC = () => {
	return (
		<div className={styles.finish}>
			<h2>Complete</h2>
		</div>
	)
}

export default Finish_card
